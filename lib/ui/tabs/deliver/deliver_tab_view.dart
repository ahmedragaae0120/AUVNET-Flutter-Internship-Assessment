import 'package:flutter/material.dart';

class DeliverTabView extends StatelessWidget {
  const DeliverTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 243, 33, 163),
    );
  }
}
